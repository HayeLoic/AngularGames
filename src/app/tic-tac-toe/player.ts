export class Player {
    id: number;
    symbol: string;
    isHuman: boolean;

    constructor(id: number, value: string, isHuman: boolean) {
        this.id = id;
        this.symbol = value;
        this.isHuman = isHuman;
    }
}